"In computing, s-expressions, sexprs or sexps (for \"symbolic expression\") are
. (y . (z . NIL)))
where NIL is the special end-of-list symbol (written '() in Scheme).In the Lisp
== Use in Lisp ==
When representing source code in Lisp, the first element of an S-expression is
commonly an operator or function name and any remaining elements are treated as
arguments. This is called \"prefix notation\" or \"Cambridge Polish notation\".
As an example, the Boolean expression written 4 == (2 + 2) in C is. 
== Section 2 ==
this is section 2
